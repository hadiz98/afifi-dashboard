/** Public paths under `/public` for brand imagery — use with `next/image` or plain `<img>`. */
export const BRAND_MEDIA = {
  logo: "/logo200x80.png",
  /** Nominal intrinsic size of the logo asset (for layout / `next/image`). */
  logoWidth: 200,
  logoHeight: 80,
} as const;
